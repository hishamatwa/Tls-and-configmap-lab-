apiVersion: v1
kind: Service
metadata:
  name: nginx-tls-service
  namespace: nginx-tls
spec:
  type: NodePort
  selector:
    app: nginx-tls-app
  ports:
  - name: https
    protocol: TCP
    port: 443
    targetPort: 443
    nodePort: 30443
